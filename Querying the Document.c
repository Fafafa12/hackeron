#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX_CHARACTERS 1005
#define MAX_PARAGRAPHS 5

/*
Alicia has sent a document to her friend Teodora as a string of characters,
	i.e. represented by char*  not char**** . Help her convert the document to cahr**** form by completing the following functions:

char	****get_document(char *text)
 to return the document represented by char ****.
char	***kth_paragraph(char ****document, int k)
 to return the  paragraph.
char	**kth_sentence_in_mth_paragraph(char ****document, int k, int m)
 to return the  sentence in the  paragraph.
char	*kth_word_in_mth_sentence_of_nth_paragraph(char ****document, int k,
 to return the  word in the  sentence of the  paragraph.
*/
char	*kth_word_in_mth_sentence_of_nth_paragraph(char ****document, int k,
		int m, int n)
{
}

char	**kth_sentence_in_mth_paragraph(char ****document, int k, int m)
{
}

char	***kth_paragraph(char ****document, int k)
{
}

char	****get_document(char *text)
{
}